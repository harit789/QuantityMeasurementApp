# Database Configuration - H2
db.url=jdbc:h2:mem:quantitydb;DB_CLOSE_DELAY=-1
db.username=sa
db.password=
db.driver=org.h2.Driver

# Connection Pool Settings
db.pool.size=5

# Repository Type: cache or database
repository.type=database